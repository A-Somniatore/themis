[workspace]
resolver = "2"
members = [
    "crates/themis-core",
    "crates/themis-openapi",
    "crates/themis-lint",
    "crates/themis-compat",
    "crates/themis-codegen",
    "crates/themis-cli",
    "crates/themis-artifact",
]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/A-Somniatore/themis"
rust-version = "1.75"

[workspace.dependencies]
# Shared platform types
themis-platform-types = { git = "https://github.com/A-Somniatore/themis-platform-types", branch = "main" }

# Core dependencies
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
thiserror = "2.0"
anyhow = "1.0"
uuid = { version = "1.0", features = ["v7", "serde"] }
chrono = { version = "0.4", features = ["serde"] }

# OpenAPI parsing
openapiv3 = "2.0"
indexmap = { version = "2.0", features = ["serde"] }

# Async runtime
tokio = { version = "1.0", features = ["full"] }

# CLI
clap = { version = "4.0", features = ["derive"] }

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Testing
proptest = "1.0"

# Workspace crates
themis-core = { path = "crates/themis-core" }
themis-openapi = { path = "crates/themis-openapi" }
themis-lint = { path = "crates/themis-lint" }
themis-compat = { path = "crates/themis-compat" }
themis-codegen = { path = "crates/themis-codegen" }
themis-artifact = { path = "crates/themis-artifact" }

# Case conversion
heck = "0.5"

[workspace.lints.rust]
unsafe_code = "forbid"
missing_docs = "warn"

[workspace.lints.clippy]
all = "warn"
pedantic = "warn"
nursery = "warn"

[profile.release]
lto = true
codegen-units = 1
